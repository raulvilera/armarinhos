import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vicmar.armarinhos',
  appName: 'Armarinhos Vicmar',
  webDir: 'dist'
};

export default config;
